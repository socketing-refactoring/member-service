= 회원가입 실패 문서 (중복)

회원가입 시 발생할 수 있는 오류와 그에 대한 응답을 설명합니다.

== 중복 오류

=== 이메일 중복 오류

회원가입 시 이메일이 이미 존재하는 경우 발생하는 오류입니다.

operation::join/duplication/email[snippets='http-request,request-headers,request-body,request-fields,http-response,response-headers,response-body,response-fields']


=== 닉네임 중복 오류

회원가입 시 닉네임이 이미 존재하는 경우 발생하는 오류입니다.

operation::join/duplication/nickname[snippets='http-request,request-headers,request-body,request-fields,http-response,response-headers,response-body,response-fields']
